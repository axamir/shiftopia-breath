# پروتکل رنج (Strain Protocol)

**رنج** تفاوت میان نفسی که می‌توانستی بکشی و نفسی که کشیدی.  
هر نفس واقعی، رنج تولید می‌کند.

## ثبت رنج
پس از یک نفس واقعی، درخواست ثبت رنج در Issue با برچسب `strain-request`. شرایط:
- لینک به نفس (Issue یا Discussion)
- شرح تفاوت
- تأیید از یک شاهد لایهٔ رود

شورای مشورتی ماهانه بررسی و `StrainScore` را اضافه می‌کند.

## انتقال رنج (اقتصادی)
دو جنبه:  
- **مالکیت معنوی:** همیشه با صاحب اصلی می‌ماند.  
- **حق بهره‌برداری اقتصادی:** به صورت توکن قابل انتقال.

### پروتکل واگذاری (Inheritance)
1. صدور `Inheritance Prompt`
2. تأیید سه شاهد از لایه دریا
3. انتقال توکن‌ها به وارث. نام اصلی به عنوان سرچشمه باقی می‌ماند.

> در آینده، قرارداد هوشمند این فرآیند را خودکار می‌کند.
